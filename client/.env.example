# Copy this file to .env to connect the client to the real API.
# Leave VITE_API_URL empty (or skip the .env file) to run in browser-only demo mode.

# Address of the Express API from ../server (its PORT)
VITE_API_URL=http://localhost:3030
